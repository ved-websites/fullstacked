{{#> container }}

	{{> preview text=(t 'auth.emails.forgot_password_request.body.preview') }}

	{{#> text }}
		{{ t 'auth.emails.forgot_password_request.body.details' }}
	{{/text}}

	{{> hr }}

	{{#> button url=url
	linkStyles=(object
		padding="10px 0"
		backgroundColor=(color "blue")
		borderRadius="3px"
		color=(color "white")
		fontSize="18px"
		textDecoration="none"
		textAlign="center"
		display="block"
		width="100%"
	)
	}}
		{{ t 'auth.emails.forgot_password_request.body.cta-button' }}
	{{/button}}

{{/container}}