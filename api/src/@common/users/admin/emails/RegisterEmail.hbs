{{#> container }}

	{{> preview text=(t 'admin.emails.register.body.preview') }}

	{{#> text }}
		{{ t 'admin.emails.register.body.greetings' (object name=name) }}
	{{/text}}

	{{> hr }}

	{{#> button url=url
	linkStyles=(object
		padding="10px 0"
		backgroundColor=(color "blue")
		borderRadius="3px"
		color=(color "white")
		fontSize="18px"
		textDecoration="none"
		textAlign="center"
		display="block"
		width="100%"
	)
	}}
		{{ t 'admin.emails.register.body.cta-button' }}
	{{/button}}

{{/container}}