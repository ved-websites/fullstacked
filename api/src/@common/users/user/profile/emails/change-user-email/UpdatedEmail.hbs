{{#> container }}

	{{> preview text=(t 'users.emails.settings.email.updated.preview') }}

	{{> textblock text=(t 'users.emails.settings.email.updated.body.text' (object name=name)) }}

{{/container}}