import { initContract } from '@ts-rest/core';

/** This is the contract that is being initialized. */
export const contract = initContract();

/** Shorthand for {@link contract} */
export const c = contract;
